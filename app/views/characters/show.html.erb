<div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
  <div class="px-4 py-6 sm:px-0">
    <div class="mb-6">
      <div class="flex justify-between items-center">
        <h1 class="text-2xl font-semibold text-gray-900">Character Details</h1>
        <%= link_to "Back to characters", characters_path, class: "text-sm text-gray-600 hover:text-gray-900" %>
      </div>
    </div>

    <%= render "sheet", character: @character %>
    <%= render "ability_scores", character: @character %>
  </div>
</div>
